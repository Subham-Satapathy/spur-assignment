# Server Configuration
PORT=3000
NODE_ENV=development

# Database Configuration (NeonDB)
DATABASE_URL=postgresql://username:password@ep-xxx-xxx.us-east-1.aws.neon.tech/neondb?sslmode=require

# OpenAI Configuration
OPENAI_API_KEY=sk-your-openai-api-key-here
OPENAI_MODEL=gpt-4

# Optional: LLM Advanced Configuration
LLM_MAX_TOKENS=500
LLM_TEMPERATURE=0.7

# Optional: Redis Configuration (for caching)
# Render free tier: 25MB RAM, 50 connection limit
REDIS_ENABLED=true
REDIS_URL=redis://red-d52qenbe5dus73bk2sj0:6379

# Application Configuration
MAX_MESSAGE_LENGTH=2000
MAX_CONVERSATION_HISTORY=10

# Tool Calling (disabled by default - architecture in place for future use)
ENABLE_TOOLS=false

# Channel Configuration (all disabled by default - architecture ready for future use)

# Telegram Channel (optional - mock implementation, set to true + add tokens to enable)
TELEGRAM_ENABLED=false
TELEGRAM_BOT_TOKEN=123456789:ABCdefGHIjklMNOpqrSTUvwxyz
TELEGRAM_WEBHOOK_SECRET=your_webhook_secret_token
TELEGRAM_WEBHOOK_URL=https://yourdomain.com/webhooks/telegram

# WhatsApp Channel (optional - mock implementation, set to true + add tokens to enable)
WHATSAPP_ENABLED=false
WHATSAPP_ACCESS_TOKEN=your_meta_access_token
WHATSAPP_PHONE_NUMBER_ID=your_phone_number_id
WHATSAPP_WEBHOOK_SECRET=your_webhook_verification_token

# Rate Limiting Configuration
RATE_LIMIT_CHAT_MAX=20
RATE_LIMIT_CHAT_WINDOW_HOURS=1
RATE_LIMIT_CONVERSATION_MAX=5
RATE_LIMIT_CONVERSATION_WINDOW_HOURS=1
RATE_LIMIT_GLOBAL_MAX=100
RATE_LIMIT_GLOBAL_WINDOW_MINUTES=15

# Cache Configuration
CACHE_CLEANUP_INTERVAL_MINUTES=5
KNOWLEDGE_CACHE_TTL_SECONDS=60
KNOWLEDGE_REDIS_TTL_SECONDS=600
